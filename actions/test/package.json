{
  "name": "@cpdevtools/git-flow-action-test",
  "version": "0.0.0-DEFAULT",
  "private": true,
  "description": "GitHub Action for Phase 4 Test orchestration",
  "scripts": {
    "build": "wireit",
    "clean": "rm -rf dist",
    "dev": "tsup --watch"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@cpdevtools/ts-dev-utilities": "^0.3.1",
    "tsx": "^4.19.2"
  },
  "devDependencies": {},
  "author": "CPDevTools",
  "license": "MIT",
  "wireit": {
    "build": {
      "command": "tsup",
      "dependencies": [
        "../../packages/git-flow:build"
      ],
      "files": [
        "src/**/*.ts",
        "tsup.config.ts"
      ],
      "output": [
        "dist/**"
      ]
    }
  }
}
